
#include <stdio.h>
#include <math.h>

int main(void)

{
    /* float lowVal1, lowVal2, hiVal1, hiVal2;

    lowVal1 = floor(18.5);
    printf(" %f", lowVal1);

    lowVal2 = floor(-18.5);
    printf(" %f", lowVal2);

    lowVal1 = ceil(18.5);
    printf(" %f", lowVal1);

    lowVal2 = ceil(-18.5);
    printf(" %f", lowVal2); */

    //printf("The absolute value of -25.5 is %.0f", fabs(-25.54));

    printf("10 raised to the power of 3 is %.0f\n", pow(10, 3));

    printf("The squared root of 64 is %.0f\n", sqrt(64));

    printf("the tangent of 45 is %f", tan(45));

}