
#include<stdio.h>
int main(void)
{
    int x, y;
    
    printf("let x be ");
    scanf("%i", &x);

    printf("let y be ");
    scanf("%i", &y);

    printf("so the sum is: %i\n ", x + y);

    return 0;
}